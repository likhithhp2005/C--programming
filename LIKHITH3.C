#include<stdio.h>
int main()
{
  int n,i,factorial;
  printf("enter a given number:");
  scanf("%d",&n);
 for(i=1;i<=n;i++)
  {factorial *=i;
  }
  printf("factorial:%d",factorial);
  return 0;
  }